# 2026-07-07 MindFlow 迭代记录

## 第36次执行 — PWA 离线支持 + DOMPurify XSS 防护

**背景**：PRD §9 非功能需求要求"PWA 基础功能完全离线可用"，安全约束要求 DOMPurify。

**改动**：
1. `vite.config.ts` — `vite-plugin-pwa` manifest + Workbox generateSW（29 entries 预缓存 + Google Fonts runtime cache）
2. `index.html` — theme-color / manifest / apple-touch-icon meta 标签
3. `src/hooks/usePWA.ts` — `beforeinstallprompt` 事件监听 + 安装到桌面能力
4. `src/components/layout/Header.tsx` — 条件显示安装按钮
5. `src/lib/sanitize.ts` — DOMPurify 封装（sanitizeText / sanitizeUrl / safeLinkUrl）
6. `src/components/mindmap/NodeDetailSidebar.tsx` — Markdown link 使用 safeLinkUrl 拦截危险协议
7. `docs/PRD.md` — §11 迭代记录补全

**验证**：Build 零 errors ✅，部署 → https://7027c992.mindflow-app.pages.dev ✅

---

## 第35次执行 — 路由级代码分割（懒加载）

**背景**：Build 输出显示首屏 bundle 550KB gzip 过大（1.7MB raw），所有页面均为静态导入。PRD 非功能需求要求"首次加载 < 2秒"。

**改动**：
1. **`src/frontend/src/App.tsx`** — React.lazy + Suspense 路由级懒加载：
   - 核心页面（Dashboard/Auth 等）除外，其余全部 lazy：ProjectMindMap/Outline/ProjectList/ProjectBoard/GlobalTasks/GlobalBoard/Calendar/Gantt/Settings
   - 统一 `PageFallback` 加载 spinner（与 auth loading 一致）
2. **`docs/PRD.md`** — §11 迭代记录补全 25~34 次（10条）
3. **提交 E2E 稳定性改进**：journey-2/3 retry 机制 + scrollIntoView，journey-6 改用 `__mindMap` API 直接操作

**验证**：
- Build 零 errors ✅
- 首屏 JS: 550KB gzip → 175KB gzip（-68%）✅
- 下游 chunk 拆分: ProjectMindMap 290KB, Settings 18KB, Gantt 3.4KB 等 ✅
- `wrangler pages deploy` 成功 → https://9a78def0.mindflow-app.pages.dev ✅

## 2026-07-07 第 37 次自动迭代
- 提交遗留未提交代码（看板添加任务联动 mindmap + 标签列移除 + 暗色配色微调）
- Sidebar 项目列表拖拽排序：HTML5 DnD，视觉反馈，reorderProjects + syncProjectToCloud
- PRD §11 迭代记录补全

## 第38次执行 — 设计重构（redesign-existing-projects skill）

**诊断发现**：
1. **字体指纹**：`@fontsource-variable/geist` 已安装但从未使用，应用从 Google Fonts 拉 Inter（AI 默认字体指纹）
2. **品牌强调色失真**：favicon/logo 是紫色(`#863bff`/`#7e14ff`)，但 UI 用靛蓝(`#4F46E5`，即 Tailwind indigo-600)——双强调色不统一
3. **纯黑阴影**：boxShadow / --shadow-* 用 `rgba(0,0,0,...)`，未与背景色相融合
4. **无纹理/深度**：纯平背景，缺颗粒/环境光

**改动（全部集中在 design tokens，无逻辑/DOM 变更）**：
1. `tailwind.config.js` — `primary` 刻度靛蓝→统一品牌紫(`#7C5CFC` 系)；`fontFamily.sans` → `Geist Variable`+CJK 回退；`boxShadow` 纯黑→紫调 `rgba(76,29,149,...)`
2. `src/index.css` — light/dark `--color-primary*`、`--border-focus`、`--primary`、`--ring`、阴影变量全部改紫调；`--font-body` 改 Geist；新增 `body::after` feTurbulence 颗粒叠加(2.5% opacity)、body 紫调环境径向渐变、`.font-display` 工具类、`a:focus-visible` 焦点环
3. `src/main.tsx` — `import '@fontsource-variable/geist'`
4. `index.html` — 移除 Inter Google Fonts link（保留 Noto Sans SC 中文）、theme-color→`#7C5CFC`、补 og/twitter meta
5. `public/favicon.svg` — `#863bff`→`#7C5CFC`、`#7e14ff`→`#6A48F0`（含对应 P3 声明）
6. `src/pages/HomePage.tsx` — 空状态 hero 焕新：`.font-display` 大标题 + dot-grid 纹理 + 紫调环境光晕 + 图标嵌于圆角 chip

**验证**：`npm run build` 零 error ✅；编译 CSS 含 `Geist Variable`/`#7c5cfc`/`feTurbulence`/`font-display` ✅；`project.indigo:#4F46E5` 为分类色刻意保留（非品牌色）。E2E 不受影响（无 DOM/逻辑改动）。
Dev server: http://localhost:5174/

## 2026-07-07 代码上云准备（未联网推送）
- 用户要求推送到 GitHub 仓库 spe-Hu/MindFLow
- 配置 git remote origin = https://github.com/spe-Hu/MindFLow
- 补全 .gitignore（此前为空，导致 node_modules/test-results/playwright-report 被误跟踪）
- 从版本控制移除整个 node_modules（约数千文件）+ test-results + playwright-report 跟踪；debug-*.mjs 临时脚本忽略
- 提交所有遗留未提交源码改动（App.tsx 懒加载 / HomePage.tsx / 今日工作日志 / package.json / index.css / tailwind.config 等）
- **环境无外网出口，无法 push**。待用户在联网机器执行：git push -u origin master

## 2026-07-07 第 38 次自动迭代 — Lint 清理 + 运行时 bug 修复
- ProjectBoardPage EmptyState 未导入修复（看板空列运行时崩溃）
- SyncStatusIndicator useEffect 条件调用修复（hooks 规则违例）
- MindMap.usePlugin() oxlint false positive 忽略注释
- 清理未使用导入：useId、toast、get
- Build 零 errors，Lint 0 errors ✅｜commit `978155a`

## 2026-07-07 第 39 次自动迭代 — 消除 INEFFECTIVE_DYNAMIC_IMPORT 构建警告
- 根因：projectStore/templates/db 被动态导入的同时也被大量组件静态导入，Vite 无法拆分为独立 chunk
- syncStore.ts: projectStore 改为同步导入（无循环依赖风险）
- aiMindMap.ts: applyTemplate/getTemplateById 改为同步导入（与 createNode 统一）
- NewProjectDialog.tsx: syncTasksFromTree 改为同步导入（db 已在顶部静态导入）
- Build 零 errors + 零 warnings ✅｜Lint 0 errors ✅｜commit `f8ac84a`
