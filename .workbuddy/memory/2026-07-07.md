# 2026-07-07 MindFlow 迭代记录

## 第36次执行 — PWA 离线支持 + DOMPurify XSS 防护

**背景**：PRD §9 非功能需求要求"PWA 基础功能完全离线可用"，安全约束要求 DOMPurify。

**改动**：
1. `vite.config.ts` — `vite-plugin-pwa` manifest + Workbox generateSW（29 entries 预缓存 + Google Fonts runtime cache）
2. `index.html` — theme-color / manifest / apple-touch-icon meta 标签
3. `src/hooks/usePWA.ts` — `beforeinstallprompt` 事件监听 + 安装到桌面能力
4. `src/components/layout/Header.tsx` — 条件显示安装按钮
5. `src/lib/sanitize.ts` — DOMPurify 封装（sanitizeText / sanitizeUrl / safeLinkUrl）
6. `src/components/mindmap/NodeDetailSidebar.tsx` — Markdown link 使用 safeLinkUrl 拦截危险协议
7. `docs/PRD.md` — §11 迭代记录补全

**验证**：Build 零 errors ✅，部署 → https://7027c992.mindflow-app.pages.dev ✅

---

## 第35次执行 — 路由级代码分割（懒加载）

**背景**：Build 输出显示首屏 bundle 550KB gzip 过大（1.7MB raw），所有页面均为静态导入。PRD 非功能需求要求"首次加载 < 2秒"。

**改动**：
1. **`src/frontend/src/App.tsx`** — React.lazy + Suspense 路由级懒加载：
   - 核心页面（Dashboard/Auth 等）除外，其余全部 lazy：ProjectMindMap/Outline/ProjectList/ProjectBoard/GlobalTasks/GlobalBoard/Calendar/Gantt/Settings
   - 统一 `PageFallback` 加载 spinner（与 auth loading 一致）
2. **`docs/PRD.md`** — §11 迭代记录补全 25~34 次（10条）
3. **提交 E2E 稳定性改进**：journey-2/3 retry 机制 + scrollIntoView，journey-6 改用 `__mindMap` API 直接操作

**验证**：
- Build 零 errors ✅
- 首屏 JS: 550KB gzip → 175KB gzip（-68%）✅
- 下游 chunk 拆分: ProjectMindMap 290KB, Settings 18KB, Gantt 3.4KB 等 ✅
- `wrangler pages deploy` 成功 → https://9a78def0.mindflow-app.pages.dev ✅

## 2026-07-07 第 37 次自动迭代
- 提交遗留未提交代码（看板添加任务联动 mindmap + 标签列移除 + 暗色配色微调）
- Sidebar 项目列表拖拽排序：HTML5 DnD，视觉反馈，reorderProjects + syncProjectToCloud
- PRD §11 迭代记录补全
