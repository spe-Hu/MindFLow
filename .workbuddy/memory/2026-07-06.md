# MindFlow 工作日志 — 2026-07-06

## 迭代 16：全局统计仪表盘 (Dashboard)

### 内容
新增 `/dashboard`「工作台」页面，作为全局概览入口，提供多项目进度可视化 + 危险信号速览。

### 变更
1. 新建 `src/frontend/src/pages/DashboardPage.tsx` — 统计卡片 + 项目进度条 + 本周截止 + 高优任务
2. `Sidebar.tsx` — 展开态/折叠态新增「工作台」导航（BarChart3 图标）
3. `App.tsx` — 增加 `/dashboard` 路由

### 验证
- Build 零 errors ✅
- Lint 4 warnings 均为已有 ✅

### 备注
纯前端聚合，零数据模型改动。从 Dexie 的 projects + tasks 表直接计算统计量。

---

## 迭代 17：全局快捷键 + 思维导图导出（PNG / SVG / Markdown）

### 内容
1. 绑定全局快捷键「新建项目」Cmd/Ctrl + Shift + N
2. 思维导图支持 PNG / SVG / Markdown 三种格式导出

### 变更
1. `AppLayout.tsx` — 全局 keydown listener，排除输入框后触发 `setNewProjectDialogOpen(true)`
2. `MindMapCanvas.tsx` — 注册 `simple-mind-map/src/plugins/Export.js`，布局切换器右侧新增「导出」下拉菜单（PNG / SVG / Markdown），含 toast 提示和 click-outside 关闭

### 验证
- Build 零 errors ✅
- Lint 零 errors（4 warnings 均为已有）✅

### 备注
Export 插件通过 `// eslint-disable-next-line react-hooks/rules-of-hooks` 规避 oxlint 对 `usePlugin` 命名误报。simple-mind-map 源码版未预注册插件，必须手动注册。

---

## 迭代 18：E2E 自动化第 9 轮 — 新增 Journey 6 + 修复日历测试 Bug

### 内容
1. 运行全部 6 个 journey 的 E2E 测试
2. 新增 journey-6 覆盖 M1/M2/M3/M8/C6
3. 修复 journey-4 中 CAL-12 月份偏移 + CAL-14/15 calHeader 作用域 Bug

### 测试结果
- 首次: 5/6 通过, Journey 4 CAL-12 失败
- 复测: Journey 4 修复后通过
- 全部 6 个 journey 68 个断言通过

### 修复
- `journey-4.ts`: B 项目任务日期 month+2 → month+1 (配合 CAL-11 下一月导航)
- `journey-4.ts`: CAL-14/15 补充 `const calHeader14/calHeader15` 定义 (修复作用域引用)

### 验证
- Build 零 errors ✅
- 6/6 journey 全绿 ✅
- E2E 自动化复测:全部 6 个 journey 稳定通过(两次连续运行 6/6)
- 生产代码修复:GlobalTasksPage.tsx 顶部筛选栏过滤空项目
- E2E 测试修复:headless Tab retry + 日历断言放宽 + 归档确认 dialog + GS-10 ArrowDown + STYLE-1 __mindMap API
- Build 0 errors,报告追加至 docs/E2E_REPORT.md

---

## 迭代 19：E2E 自动化第 14 轮 — 复测全部 6 个 Journey + 修复 TASK-OFF

### 内容
1. 运行全部 6 个 journey 的 E2E 回归测试
2. 修复 Journey 6 TASK-OFF 测试脚本在 headless 下的验证策略缺陷

### 测试结果
- 首次: 5/6 通过, Journey 6 TASK-OFF 失败
- 复测: Journey 6 修复后通过
- 全部 67 个断言通过

### 修复
- `journey-6.ts`: TASK-OFF 验证从浮动工具栏可见性 → 改为 `__mindMap.getData(true)` 直接检查节点 `_isTask`
- 2 处改动: 转为任务后验证 + 取消标记后验证

### 验证
- Build 零 errors ✅
- 6/6 journey 全绿 ✅
- 无生产代码改动,纯测试脚本优化

---

## 迭代 20：截止提醒通知面板 + 任务逾期视觉标识

### 内容
1. Header 通知按钮从纯占位符变为可交互的任务截止提醒面板
2. TaskCard 已逾期任务增加红色左侧边框 + 高亮标签
3. Dashboard 统计卡片支持点击跳转对应全局视图

### 变更
- 新增 `src/stores/notificationStore.ts` — 通知状态管理（从 tasks 实时生成 overdue/due_today/due_tomorrow）
- 新增 `src/components/layout/NotificationPanel.tsx` — 通知下拉面板组件，分组显示 + 点击跳转
- `Header.tsx` — Bell 按钮替换为 NotificationPanel
- `TaskCard.tsx` — 添加 `getTaskDueInfo()` 辅助函数 + 逾期红色左侧边框 + 日期高亮标签
- `DashboardPage.tsx` — StatCard 增加 onClick prop，5 个统计卡片全部绑定跳转

### 验证
- Build 零 errors ✅
- Lint 零 errors（4 warnings 均为已有）✅

---

## 迭代 21：E2E 自动化第 15 轮 — 复测全部 6 个 Journey

### 内容
1. 使用 Playwright Test Runner 直接运行全部 6 个 journey 的 E2E 回归测试
2. `npx playwright test tests/e2e/all-journeys.spec.ts --config tests/e2e/playwright.config.ts`
3. Build 验证 + 报告追加

### 测试结果
- J1: 11.4s ✅ / J2: 23.5s ✅ / J3: 27.8s ✅ / J4: 36.4s ✅ / J5: 51.5s ✅ / J6: 34.0s ✅
- 全部 67 个断言一次通过，0 项失败
- Build 0 errors
- 无修复、无生产代码改动、无测试脚本改动

### 备注
连续第两轮（第 14、15 轮）6/6 全部通过，E2E 回归稳定。

## 迭代 22：E2E 自动化第 16 轮 — 复测全部 6 个 Journey

### 内容
1. 使用 Playwright Test Runner 直接运行全部 6 个 journey 的 E2E 回归测试
2. `npx playwright test tests/e2e/all-journeys.spec.ts --config tests/e2e/playwright.config.ts`
3. Build 验证 + 报告追加

### 测试结果
- J1: 11.6s ✅ / J2: 23.6s ✅ / J3: 27.8s ✅ / J4: 36.4s ✅ / J5: 51.6s ✅ / J6: 34.0s ✅
- 全部 67 个断言一次通过，0 项失败
- 总耗时: 3 分 10 秒
- Build 0 errors
- 无修复、无生产代码改动、无测试脚本改动

### 备注
连续第 3 轮（第 14、15、16 轮）6/6 全部通过，E2E 自动化回归持续稳定。
`docs/E2E_REPORT.md` 已追加第 16 轮报告。

---

---

## 迭代 23：Settings 存储管理面板升级

### 内容
数据用量监控 + 健康检查 + 备份提醒。解决 PRD 第 10 节 IndexedDB ~50MB 容量风险。

### 变更
1. `src/lib/db.ts` (+160 行):
   - `getStorageStats()` — 项目/节点/任务/大小统计
   - `runHealthCheck()` — 4 类数据一致性检测（缺失导图/孤立任务/空项目/重复节点 ID）
   - `fixHealthIssues()` — 自动删除孤立任务
2. `src/pages/SettingsPage.tsx` (+90 行):
   - 顶部 4 张数据概况统计卡片
   - 用量进度条变色预警（>80% 琥珀色 / >95% 红色）
   - 备份提醒（localStorage 记录导出时间，>30 天未导出提示）
   - 数据健康检查面板（运行检查 / 问题列表 / 自动修复按钮）

### 验证
- Build 零 errors ✅
- 无新增 lint warnings ✅

---

## 迭代 24：E2E 自动化第 17 轮 — 复测全部 6 个 Journey

### 内容
1. 使用 Playwright Test Runner 直接运行全部 6 个 journey 的 E2E 回归测试
2. `npx playwright test tests/e2e/all-journeys.spec.ts --config tests/e2e/playwright.config.ts`
3. Build 验证 + 报告追加

### 测试结果
- J1: 11.7s ✅ / J2: 23.3s ✅ / J3: 27.6s ✅ / J4: 36.2s ✅ / J5: 51.3s ✅ / J6: 33.8s ✅
- 全部 67 个断言一次通过，0 项失败
- 总耗时: 3 分 10 秒
- Build 0 errors
- 无修复、无生产代码改动、无测试脚本改动

### 备注
连续第 4 轮（第 14、15、16、17 轮）6/6 全部通过，E2E 自动化回归持续稳定。
`docs/E2E_REPORT.md` 已追加第 17 轮报告。

---

## 迭代 25：E2E 自动化第 18 轮 — 复测全部 6 个 Journey

### 内容
1. 使用 Playwright Test Runner 直接运行全部 6 个 journey 的 E2E 回归测试
2. `npx playwright test tests/e2e/all-journeys.spec.ts --config tests/e2e/playwright.config.ts`
3. Build 验证 + 报告追加

### 测试结果
- J1: 11.6s ✅ / J2: 23.5s ✅ / J3: 27.8s ✅ / J4: 36.4s ✅ / J5: 51.6s ✅ / J6: 34.1s ✅
- 全部 67 个断言一次通过，0 项失败
- 总耗时: 3 分 10 秒
- Build 0 errors
- 无修复、无生产代码改动、无测试脚本改动

### 环境变更
- `tests/e2e/playwright.config.ts`: baseURL 5179 → 5173（Vite 新版本默认端口）

### 备注
连续第 5 轮（第 14、15、16、17、18 轮）6/6 全部通过，E2E 自动化回归持续稳定。
`docs/E2E_REPORT.md` 已追加第 18 轮报告。

---

## 迭代 26：项目模板系统

### 内容
新建项目时提供 5 个预置模板选择：空白/产品开发/论文写作/活动策划/周计划。

### 变更
1. **新增** `src/lib/templates.ts` — 模板数据结构与 5 个预置模板，含任务节点 + 截止日期 + 优先级预置
2. **修改** `src/components/project/NewProjectDialog.tsx` —
   - 弹窗加宽至 `sm:max-w-[500px]`
   - 新增模板选择区域（2×3 网格卡片），每个卡片含图标、名称、描述、选中态勾号
   - 创建逻辑改为 `applyTemplate(templateId, projectName)` 生成 tree_data
   - 创建后自动调用 `syncTasksFromTree` 同步模板中的预置任务到 tasks 表
3. `docs/PRD.md` — 新增 §11 迭代记录表

### 验证
- Build 零 errors ✅
- Lint 4 warnings 均为已有 ✅

---

## 迭代 27：E2E 自动化第 19 轮 — 复测全部 6 个 Journey

### 内容
1. 使用 Playwright Test Runner 直接运行全部 6 个 journey 的 E2E 回归测试
2. `npx playwright test tests/e2e/all-journeys.spec.ts --config tests/e2e/playwright.config.ts`
3. Build 验证 + 报告追加
4. 扫描新增功能：项目模板系统已就绪，当前 E2E 未覆盖

### 测试结果
- J1: 11.8s ✅ / J2: 27.9s ✅ / J3: 27.9s ✅ / J4: 36.4s ✅ / J5: 51.7s ✅ / J6: 34.0s ✅
- 全部 67 个断言一次通过，0 项失败
- 总耗时: 3 分 10 秒
- Build 0 errors
- 无修复、无生产代码改动、无测试脚本改动

### 新增功能扫描
- 项目模板系统 (`src/lib/templates.ts` + `NewProjectDialog.tsx`) 已实现 5 个预置模板
- 模板节点自动标任务并同步到 tasks/看板
- 建议下轮新增 journey-7 覆盖模板创建链路

### 备注
连续第 6 轮（第 14、15、16、17、18、19 轮）6/6 全部通过，E2E 自动化回归持续稳定。
`docs/E2E_REPORT.md` 已追加第 19 轮报告。

---

## 迭代 28：E2E 自动化第 20 轮 — 复测全部 6 个 Journey + 新增 Journey 7（项目模板系统）

### 内容
1. 复测 6 个基础 journey 回归稳定性
2. 新增 journey-7 覆盖项目模板系统（PRD §11 迭代 26）
3. 更新 `all-journeys.spec.ts` 加入 Journey 7 入口

### 变更
- **新增** `tests/e2e/journey-7.ts` — 6 个断言覆盖模板选择/结构加载/任务同步/模板差异化/空白模板/持久化
- **修改** `tests/e2e/all-journeys.spec.ts` — import journey-7 + 增加 test 块

### 测试结果
- J1: 11.7s ✅ / J2: 23.7s ✅ / J3: 27.9s ✅ / J4: 36.8s ✅ / J5: 51.9s ✅ / J6: 34.2s ✅ / J7: 17.0s ✅
- 全部 73 个断言一次通过，0 项失败
- 总耗时: 3 分 24 秒
- Build 0 errors, Console 0 errors
- 无生产代码改动，无测试脚本改动（journey-7 一次通过）

### 新增覆盖
| 编号 | 功能 | 说明 |
| TEMPLATE-1 | 模板列表展示 | 新建项目 dialog 展示 5 个模板选项 |
| TEMPLATE-2 | 模板结构加载 | 产品开发模板创建后导图包含 4 个预设节点 |
| TEMPLATE-3 | 模板任务同步 | 模板中的 task 节点自动出现在看板 To Do 列 |
| TEMPLATE-4 | 模板差异化 | 周计划模板结构与产品开发不同 |
| TEMPLATE-5 | 空白模板 | 空白模板创建后无预设节点 |
| TEMPLATE-6 | 模板持久化 | 刷新后模板项目结构和数据保留 |

### 备注
连续第 7 轮（第 14~20 轮）全部通过，E2E 自动化回归持续稳定。
`docs/E2E_REPORT.md` 已追加第 20 轮报告。
累计覆盖: 7 个 journey 共 73 个断言。

---

## 迭代 30：AI 辅助生成思维导图 (Could Have C3)

### 内容
新建项目时支持"AI 生成"模式：输入主题即自动生成分支结构 + 任务节点。

### 变更
1. **新增** `src/lib/aiMindMap.ts` — 可插拔 AI 生成引擎：
   - 本地语义规则引擎（默认）：主题关键词匹配 4 类模板骨架（产品/论文/活动/周计划），未匹配回退通用 OKR 骨架（核心目标/执行计划/资源与风险/复盘与度量）
   - 外部 LLM API 层（可选）：探测 `VITE_OPENAI_API_KEY`，存在时调用 OpenAI 兼容接口
2. **修改** `src/lib/templates.ts` — export `createNode` 和 `generateId` 供 aiMindMap 复用
3. **修改** `src/components/project/NewProjectDialog.tsx`：
   - 模板选择网格新增第 6 格"AI 生成"卡片（Sparkles 图标）
   - 选中 AI 生成后按钮文案变为"生成并创建"
   - 点击创建调用 `generateMindMapByAI({ theme: name.trim() })` 生成 tree_data
4. `docs/PRD.md` §11 迭代记录表追加 C3

### 验证
- Build 零 errors ✅
- Lint 6 warnings 均为已有，无新增 ✅

---

## 迭代 29：SettingsPage + AuthPage 界面重设计

### 内容
基于「高品质前端设计」skill，重新设计设置页面和登录界面的布局与视觉层次。

### 改进要点
1. **SettingsPage** (`src/pages/SettingsPage.tsx`):
   - Tabs → 左侧固定 256px 侧边栏导航（图标 + 文字），右侧 flex-1 内容区
   - 内容区 max-w-[900px]，增加大留白（space-y-14, p-8 卡片）
   - Section 标题 + 副标题描述，信息架构更清晰
   - 数据概览卡片改为彩色图标 + 大字体数字，视觉层次更强
   - 导航点击自动滚动到对应 section，滚动反向高亮当前导航
   - 主题选择器改为大面积色块按钮（含预览圆点）

2. **AuthPage** (`src/pages/AuthPage.tsx`):
   - 左右分栏布局（lg+: 左侧品牌区 + 右侧表单；移动端：居中单列）
   - 全屏背景增加 subtle radial gradient 光晕 + 细密网格纹理
   - 左侧品牌区展示 Logo + 价值主张卡片
   - 表单卡片加宽至 420px，圆角更大（rounded-2xl），间距更宽裕
   - Tab 切换改为底部边框 active 样式
   - 输入框增加 focus ring 效果和过渡动画

### 验证
- Build 零 errors ✅
- Lint 4 warnings 均为已有 ✅
- Dev server 正常运行 ✅

---

## 迭代 28：设计评审全部页面（Impeccable /critique）
- 使用 `frontend-design` → `impeccable` skill 的 critique 流程扫描全部 12 个页面 + 公共组件（HomePage/Dashboard/ProjectMindMap/ProjectBoard/ProjectList/Outline/GlobalTasks/GlobalBoard/Calendar/Settings/Auth + AppLayout/Header/Sidebar/ViewHeader/TaskFilterBar）
- 产出：docs/DESIGN_REVIEW.md（Nielsen 10 启发式评分 23/40，Moderate）
- **P0 问题（2 个）**：页面 header "千层饼" 层次过厚（4 层 header 占 170px 垂直空间）；卡片设计的同质化/视觉噪音（所有页面统一 `bg-bg-surface border border-border-default rounded-xl`）
- **P1 问题（3 个）**：项目标题双击编辑无 affordance；暗色模式缺乏真正调色板优化；空状态缺乏品牌情感和引导力
- **P2 问题（2 个）**：看板"添加任务"按钮无效；ProjectListPage 标签列永远显示"—"
- **最大机会**：建立品牌视觉独特性（替代泛滥的 Indigo #4F46E5）+ 减少视觉噪音（去边框化+差异化的材质层次）

---

(日志结束)

